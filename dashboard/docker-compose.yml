services:
  dashboard:
    build: .
    container_name: webserver-dashboard
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      # Docker Socket für Container-Management
      - /var/run/docker.sock:/var/run/docker.sock
      # User-Projekte (read-write für Projekt-Erstellung)
      - ../users:/app/users
      # Scripts und Templates
      - ../scripts:/app/scripts:ro
      - ../templates:/app/templates:ro
      - ../infrastructure:/app/infrastructure
      - ../config.sh:/app/config.sh:ro
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    networks:
      - webserver-network

networks:
  webserver-network:
    external: true
